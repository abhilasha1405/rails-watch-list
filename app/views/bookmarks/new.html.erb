<h3>Add to my watchlist</h3>

<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.input :comment, placeholder: 'Add comments' %>
  <%= f.association :movie, prompt: 'Select a movie', input_html: { class: 'select2' } %>
  <%= f.submit class: 'btn btn-flat' %>
<% end %>
